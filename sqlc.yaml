version: "2"
sql:
  - schema: "db/schema.sql"
    queries:
      - "db/queries/*.sql"
    engine: "postgresql"
    gen:
      go:
        package: "repo"
        out: "repo"
        sql_package: "pgx/v5"
        emit_json_tags: true
